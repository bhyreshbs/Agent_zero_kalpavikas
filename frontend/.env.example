# Frontend variables (compiled into the browser bundle — NEVER put secrets here).
# The browser talks only to the Express API; it does not call InsForge directly,
# so no InsForge URL or key is needed in the frontend.
VITE_API_BASE=/api
